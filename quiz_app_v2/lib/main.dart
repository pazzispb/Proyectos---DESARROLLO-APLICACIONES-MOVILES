import 'package:flutter/material.dart';
import 'package:quiz_app_v2/quiz.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Quiz();
  }
}
